name: bfv-netwerk

topology:
  nodes:
    node-a: { kind: linux, image: alpine:latest, exec: ["apk add --no-cache iproute2"] }
    #node-b: { kind: linux, image: alpine:latest, exec: ["apk add --no-cache iproute2"] }

    AMS: &router { kind: linux, image: alpine:latest, exec: ["apk add --no-cache iproute2"] }
    #UTR: *router
    #DEV: *router
    #APD: *router
    #ALK: *router

  links:
    - endpoints: ["node-a:eth1", "AMS:eth1"]
    #- endpoints: ["node-b:eth0", "DEV:eth0"]

    #- endpoints: ["AMS:eth1", "UTR:eth0"]
    #- endpoints: ["UTR:eth1", "DEV:eth1"]

    